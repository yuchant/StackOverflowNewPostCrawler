# Quick script showing the power of python.

Note: Does NOT use the stackexchange API, simply HTML parsing.

From start to completion in 15 minutes, including finding the Growl python bindings and determining how it works


Dependencies:
- BeautifulSoup
- Growl (osx / windows)  (now the gntp package)


Use:
- Specify a tuple of (tagname, boolean sticky) in tags attriibute.

default is...
self.tags = [('django', True), ('python', False)]

- run script: `python stack_overflow_checker`
- run as cronjob every X minutes.